// Runs after a specified interval of tym
// time in milliseconds
setTimeout(() => {}, timeout);

// Runs after a specified interval of tym but continuously
// time in milliseconds
// e.g. digital clock
setInterval(() => {}, interval);
